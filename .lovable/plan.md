

## תוכנית: כיבוי מע"מ + הסתרת שורת המע"מ בדפי תשלום מנויים

### שינויים

**1. `src/utils/discount-codes.ts`** — כיבוי החישוב:
- `addVat(sum)` → מחזיר `sum` (ללא תוספת)
- `getVatAmount(sum)` → מחזיר `0`

**2. `src/pages/Payment.tsx`** — הסתרת שורת מע"מ:
- שורה 231: להציג רק "סה״כ: ₪..." בלי "לפני מע״מ" ו"מע״מ (18%)"
- שורה 281: להסיר "המחירים אינם כוללים מע״מ"

**3. `src/pages/UpgradeSubscription.tsx`** — אותו דבר:
- שורה 195: להציג רק סה"כ
- שורה 236: להסיר הערת מע"מ

**4. `src/pages/SignupTrial.tsx`** — הסתרת שורות מע"מ:
- שורה 219: להציג "סה״כ: ₪..." במקום "כולל מע״מ"
- שורות 462-475: להסיר שורת "מע״מ (18%)" ולשנות "תשלום עתידי כולל מע״מ" ל"תשלום עתידי"

כשתרצה להפעיל מחדש — נחזיר את הנוסחאות והטקסטים.

